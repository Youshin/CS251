To compile and run : 

g++ -std=c++11 -O2 -DUSE_<C>_CHARS -o brute brute.cpp
% ./brute <encrypted> <table_filename>
g++ -std=c++11 -O2 -DUSE_<C>_CHARS -o symbol symbol.cpp
% ./symbol <encrypted> <table_filename>

I used -O2 flag while compile.